export interface Professor {
    id: number;
    nome: string;
    descricao: string;
    valor_hora: number;
    foto: string;
}//Criando a interface, quais as caracteristicas que professor tera em nosso aplicação
